{
  "name": "vr-demo",
  "description": "A proyect to show VR capabilities",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "browserify js/main.js -o js/build.js",
    "start": "budo --live --verbose --port 3000 --open"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "browserify-css": "^0.8.3",
    "budo": "^7.0.0",
    "ghpages": "0.0.3"
  },
  "dependencies": {
    "aframe-draw-component": "^1.3.0"
  }
}
